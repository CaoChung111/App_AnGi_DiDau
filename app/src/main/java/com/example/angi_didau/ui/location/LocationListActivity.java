package com.example.angi_didau.ui.location;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

/**
 * Displays a searchable list of dining locations/restaurants.
 * <p>
 * TODO: Implement list with RecyclerView + LocationListAdapter.
 * TODO: Add map view toggle (list ↔ map view).
 */
public class LocationListActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // TODO: Create activity_location_list.xml layout
        // setContentView(R.layout.activity_location_list);
    }
}
