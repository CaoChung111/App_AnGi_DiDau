package com.example.angi_didau.ui.food;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import com.example.angi_didau.R;

/**
 * Displays a searchable, filterable list of food items.
 * <p>
 * TODO: Implement list with RecyclerView + FoodListAdapter.
 * TODO: Add filtering by category, price range, and rating.
 */
public class FoodListActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // TODO: Create activity_food_list.xml layout
        // setContentView(R.layout.activity_food_list);
    }
}
